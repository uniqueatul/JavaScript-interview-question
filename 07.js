// create an object representing a car with properties making , model , and year.add a method to the car object to start the engine.

let car = {
    make: "tata",
    model: "vs6",
    year: "2025"
}

car.startEngine = function(){
    console.log("engine started")
}

(car.startEngine);

